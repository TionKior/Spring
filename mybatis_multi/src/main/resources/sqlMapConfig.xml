<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <!--加载外部properties文件-->
    <properties resource="jdbc.properties"/>

    <!--定义别名-->
    <typeAliases>
        <typeAlias type="com.tionkior.domain.User" alias="user"/>
        <typeAlias type="com.tionkior.domain.Order" alias="order"/>
        <typeAlias type="com.tionkior.domain.Role" alias="role"/>
    </typeAliases>

    <!--注册类型处理器-->
    <typeHandlers>
        <typeHandler handler="com.tionkior.handler.DateTypeHandler"/>
    </typeHandlers>

    <!--数据源环境-->
    <environments default="developement">
        <environment id="developement">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <!--加载映射文件-->
    <mappers>
        <mapper resource="com/tionkior/mapper/UserMapper.xml"/>
        <mapper resource="com/tionkior/mapper/OrderMapper.xml"/>
    </mappers>

</configuration>